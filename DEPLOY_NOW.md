# 🚀 ДЕПЛОЙ ПРЯМО СЕЙЧАС - Пошаговая инструкция

## Выбранная платформа: Render.com (Бесплатно, просто, быстро)

Render.com - лучший выбор для быстрого деплоя:
- ✅ Бесплатный тариф доступен
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка
- ✅ HTTPS включен автоматически
- ✅ Не требует кредитной карты для начала

---

## 📋 ШАГ 1: Подготовка GitHub репозитория

### Если у вас еще нет репозитория:

1. **Создайте аккаунт на GitHub** (если нет): https://github.com/signup

2. **Создайте новый репозиторий:**
   - Нажмите "New repository"
   - Название: `gymgenius-ai` (или любое другое)
   - Выберите Public или Private
   - НЕ добавляйте README, .gitignore, license (они уже есть)

3. **Загрузите код:**
   ```bash
   cd "C:\Users\Георгий\Desktop\GymGenius AI\fitness-ai-app"
   git init
   git add .
   git commit -m "Initial commit - готово к деплою"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/gymgenius-ai.git
   git push -u origin main
   ```

   ⚠️ Замените `YOUR_USERNAME` на ваш GitHub username

### Если репозиторий уже есть:

```bash
cd "C:\Users\Георгий\Desktop\GymGenius AI\fitness-ai-app"
git add .
git commit -m "Подготовка к деплою на Render"
git push
```

---

## 🌐 ШАГ 2: Деплой на Render.com

### 2.1. Регистрация на Render

1. **Зайдите на https://render.com**
2. **Нажмите "Get Started for Free"**
3. **Войдите через GitHub** (рекомендуется) или создайте аккаунт

### 2.2. Создание Web Service

1. **Нажмите "New +" → "Web Service"**

2. **Подключите репозиторий:**
   - Выберите "Connect a repository"
   - Выберите ваш репозиторий `gymgenius-ai`
   - Нажмите "Connect"

3. **Настройте сервис:**
   - **Name**: `gymgenius-backend` (или любое другое)
   - **Region**: Выберите ближайший (например, Frankfurt для Европы)
   - **Branch**: `main` (или `master`)
   - **Root Directory**: `fitness-ai-app/backend` ⚠️ ВАЖНО!
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

4. **Нажмите "Advanced" и проверьте:**
   - **Auto-Deploy**: `Yes` (автоматический деплой при push)

### 2.3. Настройка переменных окружения

1. **Прокрутите вниз до секции "Environment Variables"**

2. **Добавьте переменные:**
   - Нажмите "Add Environment Variable"
   - Добавьте:
     ```
     Key: OPENAI_API_KEY
     Value: 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
     ```
   - Нажмите "Add"
   
   - Добавьте еще одну:
     ```
     Key: ENVIRONMENT
     Value: production
     ```

3. **Нажмите "Create Web Service"**

### 2.4. Ожидание деплоя

1. **Render начнет деплой автоматически**
2. **Вы увидите логи сборки** (это займет 2-5 минут)
3. **После успешного деплоя вы получите URL:**
   - Пример: `https://gymgenius-backend.onrender.com`
   - ⚠️ Сохраните этот URL!

---

## ✅ ШАГ 3: Проверка деплоя

### 3.1. Проверка здоровья сервера

Откройте в браузере ваш URL + `/`:
```
https://your-backend-url.onrender.com/
```

Должен вернуться:
```json
{"message": "GymGenius AI API", "status": "running"}
```

### 3.2. Проверка API эндпоинтов

Проверьте в браузере или через curl:
```bash
# Health check
curl https://your-backend-url.onrender.com/

# Exercises
curl https://your-backend-url.onrender.com/exercises

# User profile
curl https://your-backend-url.onrender.com/user/profile?user_id=1
```

---

## 📱 ШАГ 4: Обновление Frontend

### 4.1. Обновите BACKEND_URL

Откройте файл `mobile/app.config.js` и замените:

```javascript
const apiUrl = 'https://your-backend-url.onrender.com';
```

⚠️ Замените `your-backend-url.onrender.com` на ваш реальный URL!

### 4.2. Пересоберите приложение

```bash
cd fitness-ai-app/mobile
npm install
npm start
```

---

## 🎉 ГОТОВО!

Ваше приложение теперь доступно по адресу:
- **Backend API**: `https://your-backend-url.onrender.com`
- **Health Check**: `https://your-backend-url.onrender.com/`
- **API Docs**: `https://your-backend-url.onrender.com/docs` (автоматически от FastAPI)

---

## 🔄 Автоматический деплой

Теперь при каждом `git push` в репозиторий, Render автоматически:
1. Обнаружит изменения
2. Пересоберет приложение
3. Задеплоит новую версию

---

## ⚠️ Важные замечания

1. **Бесплатный тариф Render:**
   - Приложение "засыпает" после 15 минут неактивности
   - Первый запрос после пробуждения может занять 30-60 секунд
   - Для production лучше использовать платный тариф ($7/месяц)

2. **База данных:**
   - SQLite файл будет создаваться автоматически
   - Данные сохраняются между деплоями
   - Для production лучше использовать PostgreSQL (Render предоставляет)

3. **Логи:**
   - Все логи доступны в Dashboard Render
   - Можно просматривать в реальном времени

---

## 🆘 Если что-то пошло не так

1. **Проверьте логи в Render Dashboard:**
   - Откройте ваш сервис
   - Перейдите в "Logs"
   - Ищите ошибки (красным цветом)

2. **Проверьте переменные окружения:**
   - Убедитесь, что `OPENAI_API_KEY` установлен
   - Проверьте формат ключа

3. **Проверьте Root Directory:**
   - Должно быть: `fitness-ai-app/backend`
   - НЕ просто `backend`!

4. **Проверьте Build Command:**
   - Должно быть: `pip install -r requirements.txt`

5. **Проверьте Start Command:**
   - Должно быть: `uvicorn main:app --host 0.0.0.0 --port $PORT`

---

## 📞 Дополнительная помощь

Если нужна помощь:
1. Проверьте `DEPLOY.md` для детальных инструкций
2. Посмотрите логи в Render Dashboard
3. Проверьте документацию Render: https://render.com/docs

---

**Удачи с деплоем! 🚀**

