# 🎯 Критически важные папки для работы приложения

## ✅ ОБЯЗАТЕЛЬНЫЕ папки (без них приложение НЕ РАБОТАЕТ):

### 1. **`backend/`** - Бэкенд сервер (КРИТИЧНО)
**Без этой папки:** Приложение не сможет работать, так как нет API сервера

**Критически важные файлы внутри:**
- `main.py` - точка входа, FastAPI приложение, все API endpoints
- `database.py` - работа с базой данных
- `openai_client.py` - интеграция с OpenAI GPT
- `plan_generator.py` - генерация планов тренировок
- `schemas.py` - модели данных (Pydantic)
- `requirements.txt` - Python зависимости

**Критически важные подпапки:**
- `uploads/` - папка для загруженных изображений (создается автоматически)

**Можно удалить (не критично):**
- `backend/__pycache__/` - скомпилированные Python файлы (пересоздадутся)

---

### 2. **`mobile/`** - Мобильное приложение (КРИТИЧНО)
**Без этой папки:** Нет пользовательского интерфейса, приложение не запустится

**Критически важные файлы внутри:**
- `App.js` - главный компонент, навигация, весь UI
- `package.json` - Node.js зависимости
- `app.json` - конфигурация Expo
- `index.js` - точка входа

**Критически важные подпапки:**
- `src/screens/` - все экраны приложения:
  - `HomeScreen.js` - главный экран
  - `CameraScreen.js` - сканирование оборудования
  - `WorkoutPlanScreen.js` - план тренировок
  - `ExerciseListScreen.js` - список упражнений
  - `ResultScreen.js` - результаты распознавания
- `i18n/` - переводы на разные языки:
  - `LanguageContext.js` - контекст языка
  - `translations.js` - файл переводов

**Можно удалить (не критично):**
- `mobile/dataconnect/` - если не используется Firebase Data Connect
- `mobile/src/dataconnect-generated/` - если не используется Firebase Data Connect
- `mobile/fitness-ai-app@1.0.0` - тестовая папка
- `mobile/expo` - временный файл Expo

---

## 📊 Минимальная структура для работы:

```
fitness-ai-app/
├── backend/                    # ✅ КРИТИЧНО
│   ├── main.py                # ✅ КРИТИЧНО
│   ├── database.py            # ✅ КРИТИЧНО
│   ├── openai_client.py       # ✅ КРИТИЧНО
│   ├── plan_generator.py      # ✅ КРИТИЧНО
│   ├── schemas.py             # ✅ КРИТИЧНО
│   └── requirements.txt       # ✅ КРИТИЧНО
│
└── mobile/                     # ✅ КРИТИЧНО
    ├── App.js                 # ✅ КРИТИЧНО
    ├── package.json           # ✅ КРИТИЧНО
    ├── app.json               # ✅ КРИТИЧНО
    ├── index.js               # ✅ КРИТИЧНО
    ├── src/screens/           # ✅ КРИТИЧНО
    └── i18n/                  # ✅ КРИТИЧНО
```

---

## 🗑️ Можно удалить (не влияет на работу):

### Папки:
- `cal-ai-app/` - альтернативное приложение (если не используется)
- `ml-model/` - ML модель (если модель уже обучена)
- `assets/` - пустая папка в корне
- `backend/functions/lib/` - скомпилированные файлы (пересоберутся)
- `backend/__pycache__/` - кеш Python (пересоздастся)
- `backend/functions/` - папка Firebase Functions (удалить)
- `backend/public/` - пустая папка (удалить)

### Файлы:
- Все `.md` файлы документации (полезны, но не критичны)
- `delete_now.py` - временный скрипт
- `DELETE_MANUALLY.txt` - инструкция

---

## 🚀 Быстрый старт (минимальный набор):

**Терминал 1 - Backend:**
```bash
cd backend
pip install -r requirements.txt
python main.py
```

**Терминал 2 - Mobile:**
```bash
cd mobile
npm install
npm start
```

**Обе папки (`backend/` и `mobile/`) должны работать одновременно!**

---

## ⚠️ ВАЖНО:

**НЕ УДАЛЯЙТЕ:**
- `backend/` - без него нет API, нет данных, нет логики
- `mobile/` - без него нет интерфейса, пользователь не может использовать приложение
- `backend/functions/src/` - если используется Firebase Functions
- `mobile/src/screens/` - все экраны приложения
- `mobile/i18n/` - переводы

**МОЖНО УДАЛИТЬ:**
- Все остальные папки из списка выше
- Документацию (`.md` файлы)
- Временные файлы и скрипты

