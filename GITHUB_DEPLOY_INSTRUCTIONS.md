# 🚀 Деплой через GitHub CLI - Инструкция

## ✅ GitHub CLI установлен и авторизован

Ваш аккаунт: **Георгий**
📧 Email: gsaprykin452@gmail.com

## 📋 Быстрый деплой

### Вариант 1: Автоматический скрипт (Рекомендуется)

1. **Запустите файл:**
   ```
   deploy-with-github-cli.bat
   ```
   
   Или в PowerShell:
   ```powershell
   powershell -ExecutionPolicy Bypass -File deploy-with-github-cli.ps1
   ```

2. **Скрипт автоматически:**
   - Проверит авторизацию GitHub
   - Инициализирует Git репозиторий (если нужно)
   - Добавит все файлы
   - Создаст коммит
   - Создаст GitHub репозиторий `gymgenius-ai`
   - Загрузит код в репозиторий

### Вариант 2: Ручной деплой через командную строку

Откройте PowerShell в папке проекта и выполните:

```powershell
# Перейдите в папку проекта
cd "C:\Users\Георгий\Desktop\GymGenius AI\fitness-ai-app"

# Инициализируйте Git (если еще не сделано)
git init
git branch -M main

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit - готово к деплою"

# Создайте репозиторий на GitHub и загрузите код
gh repo create gymgenius-ai --public --source=. --remote=origin --push
```

## 🌐 После загрузки кода на GitHub

### ШАГ 1: Деплой на Render.com

1. **Зайдите на https://render.com**
2. **Войдите через GitHub** (используйте тот же аккаунт)
3. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Выберите репозиторий: **gymgenius-ai** (или ваш GitHub username/gymgenius-ai)
   - Нажмите "Connect"

4. **Настройте сервис:**
   ```
   Name: gymgenius-backend
   Region: Frankfurt (или ближайший)
   Branch: main
   Root Directory: fitness-ai-app/backend ⚠️ ВАЖНО!
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
   ```

5. **Добавьте переменные окружения:**
   - `OPENAI_API_KEY` = ваш API ключ
   - `ENVIRONMENT` = `production`

6. **Нажмите "Create Web Service"**

7. **Дождитесь деплоя** (2-5 минут)

### ШАГ 2: Получите URL вашего сервиса

После деплоя Render предоставит URL вида:
```
https://gymgenius-backend.onrender.com
```

### ШАГ 3: Обновите Frontend

Откройте `mobile/app.config.js` и замените:
```javascript
const apiUrl = 'https://your-backend-url.onrender.com';
```

На ваш реальный URL от Render.

## ✅ Проверка

Откройте в браузере:
```
https://your-backend-url.onrender.com/
```

Должен вернуться:
```json
{"message": "GymGenius AI API", "status": "running"}
```

## 🔄 Автоматический деплой

После настройки, при каждом `git push` в репозиторий:
1. Render автоматически обнаружит изменения
2. Пересоберет приложение
3. Задеплоит новую версию

## 📞 Помощь

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что Root Directory указан правильно: `fitness-ai-app/backend`
3. Проверьте переменные окружения
4. См. подробные инструкции в `DEPLOY_NOW.md`

---

**Удачи! 🚀**

