# Структура приложения GymGenius AI

## 🎯 Основные рабочие папки

### 1. **`backend/`** - Бэкенд сервер (КРИТИЧНО)
**Назначение:** API сервер, база данных, бизнес-логика

**Ключевые файлы:**
- `main.py` - точка входа, FastAPI приложение, все API endpoints
- `database.py` - работа с базой данных (SQLite/Firestore)
- `openai_client.py` - интеграция с OpenAI GPT
- `plan_generator.py` - генерация планов тренировок
- `schemas.py` - модели данных (Pydantic)
- `requirements.txt` - Python зависимости

**Подпапки:**
- `uploads/` - загруженные изображения

**Запуск:**
```bash
cd backend
pip install -r requirements.txt
python main.py
```

**Без этой папки:** Приложение не сможет работать, так как нет API сервера

---

### 2. **`mobile/`** - Мобильное приложение (КРИТИЧНО)
**Назначение:** React Native приложение для iOS/Android

**Ключевые файлы:**
- `App.js` - главный компонент, навигация, весь UI
- `src/screens/` - все экраны приложения:
  - `HomeScreen.js` - главный экран
  - `CameraScreen.js` - сканирование оборудования
  - `WorkoutPlanScreen.js` - план тренировок
  - и другие
- `i18n/` - переводы на разные языки
- `package.json` - Node.js зависимости

**Запуск:**
```bash
cd mobile
npm install
npm start
```

**Без этой папки:** Нет пользовательского интерфейса, приложение не запустится

---

### 3. **`cal-ai-app/`** - Альтернативное приложение (ОПЦИОНАЛЬНО)
**Назначение:** Альтернативная версия на TypeScript/React

**Статус:** Похоже на экспериментальную или альтернативную версию
- Использует TypeScript вместо JavaScript
- Имеет свою структуру компонентов
- Может быть удалена, если не используется

---

### 4. **`ml-model/`** - ML модель (ОПЦИОНАЛЬНО)
**Назначение:** Обучение AI модели для распознавания оборудования

**Ключевые файлы:**
- `train_model.py` - скрипт обучения модели
- `README.md` - инструкции по обучению

**Статус:** Не критично для работы приложения, используется только для обучения модели

---

## 📊 Зависимости между папками

```
mobile/ (Frontend)
    ↓ HTTP запросы
backend/ (Backend API)
    ↓ использует
openai_client.py (GPT интеграция)
    ↓ использует
database.py (База данных)
```

## ✅ Минимальный набор для работы приложения

Для полноценной работы приложения **ОБЯЗАТЕЛЬНО нужны обе папки:**

1. **`backend/`** - без него нет API, нет данных, нет логики
2. **`mobile/`** - без него нет интерфейса, пользователь не может использовать приложение

## 🗑️ Что можно удалить (не влияет на работу)

- `cal-ai-app/` - если не используется альтернативная версия
- `ml-model/` - если модель уже обучена и не планируется переобучение
- Все тестовые папки (`g`, `gymai`, `ggg`, и т.д.)
- Документация (`.md` файлы) - полезны, но не критичны для работы

## 🚀 Быстрый старт

**Терминал 1 - Backend:**
```bash
cd backend
python main.py
```

**Терминал 2 - Mobile:**
```bash
cd mobile
npm start
```

Обе папки должны работать одновременно для полноценной работы приложения!

