# 🚀 Инструкция по запуску через Git Bash

## Быстрый запуск (2 команды)

### 1️⃣ Запуск Backend сервера

Откройте **Git Bash** и выполните:

```bash
cd "C:/Users/Георгий/Desktop/GymGenius AI/fitness-ai-app/backend"
pip install -r requirements.txt
python main.py
```

**Ожидаемый результат:**
```
INFO:     Started server process
INFO:     Uvicorn running on http://0.0.0.0:8000
```

✅ **Сервер запущен!** Оставьте это окно открытым.

---

### 2️⃣ Запуск мобильного приложения

Откройте **НОВОЕ** окно Git Bash и выполните:

```bash
cd "C:/Users/Георгий/Desktop/GymGenius AI/fitness-ai-app/mobile"
npm install
npm start
```

**Ожидаемый результат:**
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above with Expo Go
```

✅ **Приложение готово!** Вы увидите QR-код в терминале.

---

## 📱 Запуск на устройстве

### Вариант A: На физическом телефоне (рекомендуется)

1. **Скачайте Expo Go:**
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **Настройте BACKEND_URL** (важно для физических устройств):
   
   Сначала узнайте IP адрес вашего компьютера:
   ```bash
   ipconfig | grep "IPv4"
   ```
   
   Найдите строку типа: `IPv4 Address. . . . . . . . . . . : 192.168.1.100`
   
   Затем установите переменную окружения перед запуском:
   ```bash
   export BACKEND_URL="http://192.168.1.100:8000"
   npm start
   ```
   
   ⚠️ **Важно:** Замените `192.168.1.100` на ваш реальный IP адрес!

3. **Откройте Expo Go** и отсканируйте QR-код из терминала

4. **Убедитесь**, что телефон и компьютер в одной Wi-Fi сети

### Вариант B: На эмуляторе Android

В терминале, где запущен `npm start`, нажмите клавишу **`a`**

### Вариант C: На симуляторе iOS (только Mac)

В терминале, где запущен `npm start`, нажмите клавишу **`i`**

### Вариант D: В браузере (Web версия)

В терминале, где запущен `npm start`, нажмите клавишу **`w`**

---

## 🔧 Использование готовых скриптов

Для удобства можно использовать готовые скрипты:

### Запуск Backend:
```bash
cd "C:/Users/Георгий/Desktop/GymGenius AI/fitness-ai-app"
bash start-backend.sh
```

### Запуск Mobile:
```bash
cd "C:/Users/Георгий/Desktop/GymGenius AI/fitness-ai-app"
bash start-mobile.sh
```

---

## 🛠️ Решение проблем

### Проблема: `pip install` не работает
```bash
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### Проблема: `npm install` не работает
```bash
cd mobile
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Expo не запускается
```bash
cd mobile
rm -rf node_modules .expo
npm install
npm start -- --clear
```

### Проблема: Сервер не подключается с телефона
1. Убедитесь, что BACKEND_URL установлен правильно
2. Проверьте, что телефон и компьютер в одной Wi-Fi сети
3. Отключите брандмауэр Windows или добавьте исключение для порта 8000
4. Попробуйте использовать IP адрес вместо localhost

### Проблема: Камера не работает
- **iOS:** Настройки → Expo Go → Камера → Разрешить
- **Android:** Нажмите кнопку "Предоставить разрешение" при первом запуске

---

## 📋 Полная последовательность команд

### Терминал 1 (Backend):
```bash
cd "C:/Users/Георгий/Desktop/GymGenius AI/fitness-ai-app/backend"
pip install -r requirements.txt
python main.py
```

### Терминал 2 (Mobile):
```bash
cd "C:/Users/Георгий/Desktop/GymGenius AI/fitness-ai-app/mobile"

# Для физического устройства - узнайте IP и установите:
export BACKEND_URL="http://ВАШ_IP:8000"

npm install
npm start
```

---

## ✅ Проверка работы

### Проверка Backend:
Откройте браузер: http://localhost:8000/docs

### Проверка Mobile:
- Отсканируйте QR-код в Expo Go
- Или нажмите `w` для запуска в браузере

---

## 🎯 Готово!

Теперь ваше приложение должно работать! 

**Удачного использования! 💪**





