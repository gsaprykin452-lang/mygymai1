# 🔧 Устранение проблем с веб-версией

## ❌ Ошибка: "web support but don't have the required dependencies"

### Проблема:
```
CommandError: It looks like you're trying to use web support but don't have the required
dependencies installed.

Please install react-native-web@~0.19.6, react-dom@18.2.0 by running:

npx expo install react-native-web@~0.19.6 react-dom@18.2.0
```

### ✅ Решение:

1. **Перейдите в папку mobile:**
   ```bash
   cd mobile
   ```

2. **Установите веб-зависимости:**
   ```bash
   npx expo install react-native-web@~0.19.6 react-dom@18.2.0
   ```

3. **Запустите веб-версию:**
   ```bash
   npm run web
   ```

## ❌ Ошибка: "Проект неправильно настроен для статического рендеринга"

### Проблема:
```
Внутренняя ошибка:
Проект неправильно настроен для статического рендеринга (подробнее см. в терминале):
в проекте не установлена ​​версия expo-router с поддержкой Node.js.

Невозможно найти модуль '@expo/metro-runtime/symbolicate'.
```

### ✅ Решение:

1. **Убедитесь, что в app.json убрана настройка статического рендеринга:**
   ```json
   "web": {
     "favicon": "./assets/favicon.png",
     "bundler": "metro",
     "name": "GymGenius AI",
     "shortName": "GymGenius",
     "description": "AI-Powered Fitness Equipment Recognition App",
     "themeColor": "#1a1a1a",
     "backgroundColor": "#1a1a1a",
     "display": "standalone",
     "orientation": "portrait"
   }
   ```
   **НЕ должно быть:** `"output": "static"`

2. **Установите недостающие зависимости:**
   ```bash
   npx expo install @expo/metro-runtime
   ```

3. **Очистите кэш и перезапустите:**
   ```bash
   npm start -- --clear
   ```

## ❌ Проблема: Белый экран в веб-версии

### Проблема:
Веб-версия показывает белый экран вместо содержимого приложения.

### ✅ Решение:

1. **Обновите зависимости навигации:**
   ```bash
   npm install @react-navigation/native@^6.1.9 @react-navigation/stack@^6.3.20
   ```

2. **Очистите кэш и перезапустите:**
   ```bash
   npm start -- --clear
   ```

3. **Проверьте консоль браузера** (F12) на наличие ошибок JavaScript

4. **Если проблема не решается, проверьте:**
   - Все зависимости установлены корректно
   - Бэкенд запущен
   - Нет ошибок CORS

## ❌ Проблема: Expo Doctor ошибки

### Проблема:
```
11/15 checks passed. 4 checks failed.
✖ Check for common project setup issues
✖ Check Expo config schema
✖ Check that required peer dependencies are installed
✖ Check if the project meets version requirements
```

### ✅ Решение:

1. **Установите недостающие зависимости:**
   ```bash
   npm install react-native-gesture-handler@~2.14.0
   ```

2. **Обновите Expo SDK до версии 54:**
   ```bash
   npm install expo@~54.0.0
   ```

3. **Обновите все зависимости:**
   ```bash
   npm install
   ```

4. **Очистите кэш:**
   ```bash
   npm start -- --clear
   ```

**Подробное решение:** см. `EXPO_DOCTOR_FIXES.md`

## ❌ Проблема: Expo Go ругается на версию SDK

### Проблема:
```
Expo Go requires SDK 54.0.0, but project uses SDK 50.0.0
```

### ✅ Решение:

1. **Обновите до SDK 54:**
   ```bash
   npm install expo@~54.0.0
   ```

2. **Обновите React Native:**
   ```bash
   npm install react-native@0.76.3
   ```

3. **Обновите все зависимости:**
   ```bash
   npm install
   ```

4. **Очистите кэш:**
   ```bash
   npm start -- --clear
   ```

**Подробное решение:** см. `SDK_54_UPDATE.md`

## ❌ Ошибка: PlatformConstants не найден

### Проблема:
```
Invariant Violation: TurboModuleRegistry getEnforcing (...):
'PlatformConstants' could not be found.
Verify that a module by this name is registered in the native binary.
```

### ✅ Решение:

1. **Установите совместимые версии:**
   ```bash
   npm install react-native@0.75.4
   npm install @react-navigation/native@^6.1.18 @react-navigation/native-stack@^6.11.0
   ```

2. **Очистите кэш:**
   ```bash
   npm start -- --clear
   ```

3. **Перезапустите Expo Go** на мобильном устройстве

**Подробное решение:** см. `PLATFORMCONSTANTS_FIX.md`

## ❌ Ошибка: Bridgeless Mode PlatformConstants

### Проблема:
```
Invariant Violation: TurboModuleRegistry getEnforcing(...):
'PlatformConstants' could not be found.
Bridgeless mode: true. TurboModule interop: false.
```

### ✅ Решение:

1. **Очистите кэш и перезапустите:**
   ```bash
   npm start -- --clear
   ```

2. **Перезапустите Expo Go** на мобильном устройстве

3. **Если проблема не решается, проверьте конфигурацию:**
   - metro.config.js должен отключать Bridgeless mode
   - babel.config.js должен иметь правильные плагины
   - app.config.js должен быть вместо app.json

**Подробное решение:** см. `BRIDGELESS_FIX.md`

## ❌ Проблема: Expo все еще выдает ошибки

### Проблема:
После всех исправлений Expo продолжает выдавать ошибки.

### ✅ Радикальное решение:

1. **Полный сброс:**
   ```bash
   cd mobile
   rm -rf node_modules .expo package-lock.json
   ```

2. **Переустановка:**
   ```bash
   npm install
   npx expo install --fix
   ```

3. **Запуск с очисткой кэша:**
   ```bash
   npm start -- --clear
   ```

4. **Перезапуск Expo Go** на мобильном устройстве

**Подробное решение:** см. `EXPO_RESET_GUIDE.md`

## ❌ Ошибка: "Cannot resolve module"

### Проблема:
Модули не найдены после установки зависимостей.

### ✅ Решение:

1. **Очистите кэш:**
   ```bash
   npm start -- --clear
   ```

2. **Или переустановите зависимости:**
   ```bash
   rm -rf node_modules
   npm install
   ```

## ❌ Ошибка: "CORS policy"

### Проблема:
Браузер блокирует запросы к API из-за CORS.

### ✅ Решение:

1. **Убедитесь, что бэкенд запущен:**
   ```bash
   cd backend
   python main.py
   ```

2. **Проверьте, что API доступен:**
   Откройте `http://localhost:8000` в браузере

3. **Проверьте CORS настройки в backend/main.py:**
   ```python
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["*"],  # Разрешить все источники
       allow_credentials=True,
       allow_methods=["*"],
       allow_headers=["*"],
   )
   ```

## ❌ Ошибка: "Image upload failed"

### Проблема:
Не удается загрузить изображение.

### ✅ Решение:

1. **Проверьте формат файла:**
   - Поддерживаются: JPG, JPEG, PNG
   - Максимальный размер: 10MB

2. **Проверьте подключение к интернету**

3. **Попробуйте другое изображение**

## ❌ Ошибка: "Port already in use"

### Проблема:
Порт 19006 уже используется.

### ✅ Решение:

1. **Найдите процесс, использующий порт:**
   ```bash
   netstat -ano | findstr :19006
   ```

2. **Завершите процесс:**
   ```bash
   taskkill /PID <PID> /F
   ```

3. **Или используйте другой порт:**
   ```bash
   npm run web -- --port 3000
   ```

## ❌ Ошибка: "Module not found: expo-image-picker"

### Проблема:
Модуль expo-image-picker не найден.

### ✅ Решение:

1. **Установите модуль:**
   ```bash
   npx expo install expo-image-picker
   ```

2. **Обновите app.json:**
   ```json
   {
     "expo": {
       "plugins": [
         "expo-camera",
         "expo-image-picker"
       ]
     }
   }
   ```

## 🔍 Общие советы по отладке:

### 1. Проверьте консоль браузера
- Откройте Developer Tools (F12)
- Посмотрите на ошибки в Console и Network

### 2. Проверьте терминал
- Смотрите на ошибки в терминале где запущен `npm run web`

### 3. Проверьте бэкенд
- Убедитесь, что API сервер запущен
- Проверьте логи бэкенда

### 4. Очистите кэш
```bash
npm start -- --clear
```

### 5. Перезапустите все сервисы
1. Остановите веб-сервер (Ctrl+C)
2. Остановите бэкенд (Ctrl+C)
3. Запустите бэкенд заново
4. Запустите веб-сервер заново

## 📞 Если ничего не помогает:

1. **Проверьте версии:**
   - Node.js: `node --version` (должна быть 16+)
   - npm: `npm --version`
   - Expo CLI: `expo --version`

2. **Обновите зависимости:**
   ```bash
   npx expo install --fix
   ```

3. **Создайте новый проект для тестирования:**
   ```bash
   npx create-expo-app test-web --template blank
   cd test-web
   npm run web
   ```

---

**Если проблема не решается, проверьте документацию Expo: https://docs.expo.dev/workflow/web/**
